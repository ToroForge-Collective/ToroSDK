export type GetProjectInput = {
  admin: string;
  getbalances?: string;
};

export type ProductInput = {
  productId: string;
  productName: string;
  description: string;
  productImage: string;
  admin: string;
  adminpwd: string;
};

